const knex = require('../database/connection');

const basketController = {
  async byUser(req, res) {
    // auth
    // all baskets from user
  },
  async byId(req, res) {},
  async create(req, res) {
    // auth
  },
  async update(req, res) {
    // auth
  },
  async remove(req, res) {
    // auth
  }
};

module.exports = basketController;
